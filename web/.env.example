# Copie para .env.local (não commitar segredos).
# Na Vercel: Project → Settings → Environment Variables.

# URL canônica do site (sitemap, robots, metadataBase, OG)
SITE_URL=https://tudosobrecannabis.com

# Opcional: liberar /esteira em produção com Basic Auth.
# Se vazio em produção, /esteira e /api/esteira respondem 404.
# ESTEIRA_BASIC_USER=
# ESTEIRA_BASIC_PASS=
